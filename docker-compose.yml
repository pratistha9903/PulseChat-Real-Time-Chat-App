services:
  mongodb:
    image: mongo:7
    container_name: pulsechat-mongo
    ports:
      - "27017:27017"
    volumes:
      - pulsechat_mongo_data:/data/db
    restart: unless-stopped

volumes:
  pulsechat_mongo_data:
